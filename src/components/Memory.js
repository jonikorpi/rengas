import React, { Component } from "react";
import Tile from "./Tile";
import Unit from "./Unit";
import { rules } from "../shared/games.js";

export default class Memory extends Component {
  render() {
    // const { visibleTiles } = this.props.player;
    //
    // return Object.keys(visibleTiles).map(x => {
    //   return Object.keys(visibleTiles[x]).map(y => {
    //     return <Tile key={`${x},${y}`} {...this.props} x={x} y={y} />;
    //   });
    // });

    return null;
  }
}
